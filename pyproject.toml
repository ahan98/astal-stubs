[project]
name = "astal-stubs"
version = "0.1"
description = "Extra Typing Stubs for PyGObject"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gi-stubs"]
